## 1. 项目初始化与配置
- [ ] 检查并初始化 React + TypeScript + Vite 项目。
- [ ] 确保 Tailwind CSS 安装并配置完毕。
- [ ] 引入 `Space Grotesk` 等现代字体。
- [ ] 安装依赖 `lucide-react`, `clsx`, `tailwind-merge`。

## 2. 核心游戏引擎 (Hook) 开发
- [ ] 实现 `useSnakeGame` 自定义 Hook。
- [ ] 定义基础状态：蛇身坐标数组、食物坐标、当前方向、游戏状态（未开始、进行中、已结束）、得分。
- [ ] 实现游戏主循环（`useEffect` 结合 `useRef` 控制速度）。
- [ ] 实现移动逻辑与碰撞检测逻辑（撞墙、撞自身）。
- [ ] 实现吃食物逻辑：蛇身增长、得分增加、在不与蛇身重合的随机空白位置重新生成食物。
- [ ] 阻止连续反向按键导致直接自身碰撞（如当前向右，快速按左键应当无效）。

## 3. UI 组件开发
- [ ] 开发 `Header` 组件，展示霓虹标题、当前得分与历史最高分。
- [ ] 开发 `Board` 组件，渲染带有发光网格的背景。
- [ ] 开发 `Snake` 组件，根据坐标渲染蛇头与蛇身，并加入高饱和度发光样式。
- [ ] 开发 `Food` 组件，添加呼吸灯或跳动动画效果。
- [ ] 开发 `Overlay` 浮层组件，用于展示“开始游戏”、“游戏结束”界面与发光按钮。

## 4. 响应式与交互优化
- [ ] 绑定桌面端键盘事件（`ArrowUp`, `ArrowDown`, `ArrowLeft`, `ArrowRight` 及 `WASD`）。
- [ ] 添加移动端滑动手势支持（Touch Events 或 `react-swipeable` 的简单实现）。
- [ ] 优化移动端布局，防止触发浏览器默认行为（如 Safari 下拉刷新或页面滚动）。
- [ ] 实现最高分的 LocalStorage 持久化存储。

## 5. 测试与视觉打磨
- [ ] 调整颜色、发光强度（`box-shadow`, `text-shadow`），确保“赛博朋克霓虹”视觉风格完美呈现。
- [ ] 测试各种边界情况（如游戏胜利/网格全满、边界极限操作）。
- [ ] 代码清理、格式化。
